#include "main.h"
/**
 * mul - a function that multiply
 *
 * @a: first variable to be checked
 *
 * @b: second variable to be checked
 *
 * Return: return the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
